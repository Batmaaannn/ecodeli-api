export enum DeliveryType {
  FULL = "full",
  PARTIAL = "partial",
}

export enum DeliveryStatus {
  ASSIGNED = "assigned",
  PICKED_UP = "picked_up",
  IN_TRANSIT = "in_transit",
  DELIVERED = "delivered",
}
