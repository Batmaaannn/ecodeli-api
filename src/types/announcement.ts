export enum AnnouncementStatus {
  POSTED = 'posted',
  MATCHED = 'matched',
  IN_PROGRESS = 'in_progress',
  DELIVERED = 'delivered',
  CANCELLED = 'cancelled'
}

export enum AnnouncementType {
  PACKAGE = 'package',
  SERVICE = 'service',
  TRANSPORT = 'transport'
}